export const DEFAULT_TREE_WIDTH = 280;
export const MIN_TREE_WIDTH = 180;
export const MAX_TREE_WIDTH = 620;
/** Width the preview keeps when the tree is dragged wide. */
export const MIN_PREVIEW_WIDTH = 240;
